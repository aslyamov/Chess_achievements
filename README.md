# Chess Achievements 🏅

**[▶ Открыть приложение](https://aslyamov.github.io/Chess_achievements/)**

Веб-приложение для учителя шахмат — выдавайте медали ученикам, отслеживайте их достижения и смотрите персональные страницы с историей наград.

## Возможности

- **Ученики** — список учеников в виде карточек или таблицы, быстрое добавление и удаление
- **Медали** — категории и медали с картинками (URL или готовые SVG-пресеты)
- **Выдача** — кнопка 🏆 прямо в списке учеников открывает быстрый модал с выбором медали и даты
- **Профиль ученика** — все медали с фильтрами по периоду (день / неделя / месяц / год / всё время), повторные награды показываются со счётчиком `N ×`
- **Импорт / Экспорт** — скачать и загрузить всю базу одним JSON-файлом
- **Тёмная тема** — переключатель 🌙 / ☀️ в навигации, настройка сохраняется

## Технологии

| Слой | Инструмент |
|---|---|
| Язык | TypeScript |
| Сборка | esbuild |
| Стили | Tailwind CSS (CDN) |
| Хранение | localStorage |
| Роутинг | Hash-based SPA (самописный) |

## Запуск

```bash
npm install
npm run build   # одноразовая сборка
npm run dev     # watch-режим с sourcemap
```

Затем открыть `index.html` в браузере (через Live Server или просто `file://`).

## Структура проекта

```
├── index.html          # Точка входа, Tailwind CDN, навигация
├── build.js            # Конфиг esbuild
├── img/                # Предустановленные SVG-медали
│   ├── medal-gold-1.svg
│   ├── medal-gold-2.svg
│   ├── medal-silver-1.svg
│   ├── medal-silver-2.svg
│   ├── medal-bronze-1.svg
│   └── medal-bronze-2.svg
└── src/
    ├── main.ts             # Точка входа, маршруты
    ├── types.ts            # Интерфейсы данных
    ├── storage.ts          # localStorage + import/export
    ├── router.ts           # Hash-роутер
    ├── modal.ts            # Модальные окна
    └── pages/
        ├── students.ts     # Список учеников
        ├── achievements.ts # Управление медалями
        ├── assign.ts       # Выдача медали
        ├── profile.ts      # Профиль ученика
        └── importExport.ts # Импорт / Экспорт
```

## Модель данных

```typescript
Student             { id, name, createdAt }
AchievementCategory { id, name }
Achievement         { id, categoryId, name, description, imageUrl }
StudentAchievement  { id, studentId, achievementId, grantedAt }
```

Все данные хранятся в `localStorage` под ключом `chess_achievements_data`.
